@{
    ViewBag.Title = "Crear/Modificar Categoría Subdistribuidor";
    Model.SubDistribuidor subDistribuidor = ViewBag.subDistribuidor;
}

<div class="page-header">
    <h3>@ViewBag.Title</h3>
</div>

<div class="container">
    <input type="hidden" id="pagina" value="@ViewBag.pagina" />
    <input type="hidden" value="@ViewBag.debug" />
    <form class="form-horizontal">
        <div class="row">
            <input type="hidden" id="idSubDistribuidor" name="idSubDistribuidor" value="@subDistribuidor.idSubDistribuidor" />

            <div class="row">
                <div class="col-sm-12 col-lg-4">
                    <div class="form-group">
                        @Html.LabelFor(dv => subDistribuidor.codigo, htmlAttributes: new { @class = "control-label col-xs-3 col-lg-6" })
                        <div class="col-xs-9">
                            @Html.TextBoxFor(c => subDistribuidor.codigo, htmlAttributes: new { @class = "form-control" })
                        </div>
                    </div>
                </div>
                
                <div class="col-sm-12 col-lg-8">
                    <div class="form-group">
                        @Html.LabelFor(dv => subDistribuidor.nombre, htmlAttributes: new { @class = "control-label col-xs-3" })
                        <div class="col-xs-9">
                            @Html.TextBoxFor(c => subDistribuidor.nombre, htmlAttributes: new { @class = "form-control" })
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-sm-6 col-lg-6">
                <div class="form-group">
                    @Html.LabelFor(c => subDistribuidor.Estado, htmlAttributes: new { @class = "control-label col-xs-3" })
                    <div class="col-xs-9">
                        <div class="radio radio-inline">
                            <label class="radio-label">
                                @if (subDistribuidor.Estado == 1)
                                {
                                    <input class="radio-input" checked type="radio" name="subDistribuidor_estado" id="subDistribuidor_estado_si" value="1"><span>Activo</span>
                                }
                                else
                                {
                                    <input class="radio-input" type="radio" name="subDistribuidor_estado" id="subDistribuidor_estado_si" value="1"><span>Activo</span>
                                }

                            </label>
                        </div>
                        <div class="radio radio-inline">
                            <label class="radio-label">
                                @if (subDistribuidor.Estado == 0)
                                {
                                    <input class="radio-input" checked type="radio" name="subDistribuidor_estado" id="subDistribuidor_estado_no" value="0"><span>Inactivo</span>
                                }
                                else
                                {
                                    <input class="radio-input" type="radio" name="subDistribuidor_estado" id="subDistribuidor_estado_no" value="0"><span>Inactivo</span>
                                }
                            </label>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="pull-right">
                <button type="button" id="btnCancelarSubDistribuidor" class="btn btn-danger">Cancelar</button>
                <button id="btnFinalizarEdicionSubDistribuidor" type="button" class="btn btn-success">Finalizar Creación</button>
            </div>
        </div>

    </form>
</div>



@section Scripts {
    @Scripts.RenderFormat("<script type=\"text/javascript\" src=\"{0}?nocache=" + TempData["ScriptVersion"] + "\"></script>", "~/bundles/subDistribuidor")
}