@{
    ViewBag.Title = "Carga Masiva de Productos para la WEB";
}

<div class="page-header">
    <h3>@ViewBag.Title</h3>
</div>

<div class="container">
    <form action="@Url.Action("LoadProductosWeb", "Producto")" method="post" enctype="multipart/form-data" id="formCargarProductos">
        <div class="row">
            <div class="col-sm-12 col-lg-6">
                <br />
                <div class="row">
                    <div class="col-sm-12 col-lg-12">
                        <p>
                            <b>FORMATO</b>
                            <ul>
                                <li>
                                    Este formulario permite agregar/retirar de la venta por WEB productos existentes, desde un archivo Excel.
                                    Puede descargar productos en formato de actualización en el <a target="_blank" href="@Url.Action("ProductosWeb", "Producto")">formulario de búsqueda productos.</a>
                                </li>
                            </ul>
                        </p>
                    </div>
                </div>
                <br />
                @*<div class="row">
            <div class="col-sm-12 col-lg-12">


                Fecha Inicio Vigencia
                <label class="control-label col-xs-3" for="file">Inicio <br />Vigencia:</label>
                <div class="col-xs-3">
                    <input id="fechaInicioVigenciatmp" type="hidden" value="@DateTime.Now.ToString("dd/MM/yyyy")" />
                    <input class="form-control" type="text" id="fechaInicioVigencia" name="fechaInicioVigencia">
                </div>
            </div>
        </div>
        <br />*@
                <div class="row">
                    <div class="col-sm-12 col-lg-12">
                        <!--Archivo-->
                        <label class="control-label col-xs-3" for="file">Archivo Productos:</label>
                        <div class="col-xs-9">
                            <input class="form-control" type="file" name="file" id="file" />
                        </div>
                    </div>
                </div>

                <br />
                <div class="row">
                    <div class="col-sm-12 col-lg-12">
                        
                        <div class="col-xs-12 text-right">
                            <button id="btnCargarProductos" type="submit" class="btn btn-primary">Cargar Productos WEB</button>
                        </div>
                    </div>
                </div>

                <br />
                <div class="row">
                    <div class="col-sm-12 col-lg-12 text-left">

                    </div>
                </div>
            </div>
        </div>

    </form>
</div>

<script type="text/javascript">

</script>
