@{
    ViewBag.Title = "Crear/Modificar Origen";
    Model.Origen origen = ViewBag.origen;
}

<div class="page-header">
    <h3>@ViewBag.Title</h3>
</div>

<div class="container">
    <input type="hidden" id="pagina" value="@ViewBag.pagina" />
    <input type="hidden" value="@ViewBag.debug" />
    <form class="form-horizontal">
        <div class="row">
            <input type="hidden" id="idOrigen" name="idOrigen" value="@origen.idOrigen" />

            <div class="row">
                <div class="col-sm-12 col-lg-4">
                    <div class="form-group">
                        @Html.LabelFor(dv => origen.codigo, htmlAttributes: new { @class = "control-label col-xs-2 col-lg-6" })
                        <div class="col-xs-6">
                            @Html.TextBoxFor(c => origen.codigo, htmlAttributes: new { @class = "form-control" })
                        </div>
                    </div>
                </div>
                
                <div class="col-sm-12 col-lg-8">
                    <div class="form-group">
                        @Html.LabelFor(dv => origen.nombre, htmlAttributes: new { @class = "control-label col-xs-3" })
                        <div class="col-xs-9">
                            @Html.TextBoxFor(c => origen.nombre, htmlAttributes: new { @class = "form-control" })
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-sm-6 col-lg-6">
                <div class="form-group">
                    @Html.LabelFor(c => origen.Estado, htmlAttributes: new { @class = "control-label col-xs-4" })
                    <div class="col-xs-8">
                        <div class="radio radio-inline">
                            <label class="radio-label">
                                @if (origen.Estado == 1)
                                {
                                    <input class="radio-input" checked type="radio" name="origen_estado" id="origen_estado_si" value="1"><span>Activo</span>
                                }
                                else
                                {
                                    <input class="radio-input" type="radio" name="origen_estado" id="origen_estado_si" value="1"><span>Activo</span>
                                }

                            </label>
                        </div>
                        <div class="radio radio-inline">
                            <label class="radio-label">
                                @if (origen.Estado == 0)
                                {
                                    <input class="radio-input" checked type="radio" name="origen_estado" id="origen_estado_no" value="0"><span>Inactivo</span>
                                }
                                else
                                {
                                    <input class="radio-input" type="radio" name="origen_estado" id="origen_estado_no" value="0"><span>Inactivo</span>
                                }
                            </label>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="pull-right">
                <button type="button" id="btnCancelarOrigen" class="btn btn-danger">Cancelar</button>
                <button id="btnFinalizarEdicionOrigen" type="button" class="btn btn-success">Finalizar Creación</button>
            </div>
        </div>

    </form>
</div>



@section Scripts {
    @Scripts.RenderFormat("<script type=\"text/javascript\" src=\"{0}?nocache=" + TempData["ScriptVersion"] + "\"></script>", "~/bundles/origen")
}