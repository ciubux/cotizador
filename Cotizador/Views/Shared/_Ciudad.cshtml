@model Cotizador.Models.CiudadViewModels

@if (Model.incluirSeleccione)
{
@Html.DropDownList(
            Model.CiudadSelectId,
            Model.Ciudades, "Seleccione la sede",
            new
            {
                id = Model.CiudadSelectId,
                @class = "form-control"
            })
}
else
{
@Html.DropDownList(
        Model.CiudadSelectId,
        Model.Ciudades,
        new
        {
            id = Model.CiudadSelectId,
            @class = "form-control"
        })
}